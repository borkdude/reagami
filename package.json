{
  "name": "reagami",
  "version": "0.0.8",
  "dependencies": {
    "squint-cljs": "^0.9.176"
  },
  "type": "module",
  "main": "reagami.mjs",
  "exports": "./reagami.mjs",
  "keywords": [
    "reagent",
    "hiccup",
    "squint",
    "clojure",
    "clojurescript"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "esbuild": "^0.25.11",
    "jsdom": "^27.0.1"
  },
  "files": [
    "reagami.mjs"
  ],
  "scripts": {
    "build": "pnpm squint compile",
    "build:test": "pnpm squint compile --paths test --output-dir test",
    "test": "pnpm build && pnpm build:test && pnpm squint run test/test_runner.cljs"
  }
}
